# dockerfile-python-template
Template for building a docker image using docker-compose.

## Info
This sets up a basic python3.10 image which installs the requirements.txt file and runs the main.py file.


## Run Instructions 
1. Build and run docker `docker-compose up`
2. Thats it OMG.

